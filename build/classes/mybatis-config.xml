<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
 PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!-- 与Spring 整合之后，不再需要用该环境配置，只需在Spring配置文件中为SqlSessionFactoryBean(SqlSessionFactory的实现类)指定数据源即可 
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="com.mysql.cj.jdbc.Driver" />
				<property name="url" value="jdbc:mysql://localhost:3306/springtestdb?serverTimezone=UTC" />
				<property name="username" value="root" />
				<property name="password" value="123456" />
			</dataSource>
		</environment>     
	</environments>     -->
	
	
	
	<mappers>
		<mapper resource="gzc/mapper/MyUserMapper.xml" />
	</mappers>
</configuration>